import { createElement } from "../../helpers/generic/domElements"

function render(){
    const navbarItemDropdown = createElement("ul", {
        classes: "feature-navbar-item-dropdown"
    })
}

export { render as navbarItemDropdown }